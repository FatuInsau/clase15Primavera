// condicional

// si la condicion es verdadera, entonces...
if (condicion) {
    // codigo que se ejecuta cuando la condicion es verdadera

// sino, ejecuta lo siguiente...
} else {
    // codigo para cuando la condicion es falsa
}

(condicion) ? condicionEsVerdadera : condicionEsFalsa

if (condicion) {
    // codigo
} else if (condicion2) {
    // codigo
} else {
    // codigo
}

// switch

// según
switch (nombre) {
    case "Belen":
        console.log("hola belen");
        break;
    case "Fati":
        console.log("hola fati");
        break;
    default: 
        console.log("No te conozco");
};

